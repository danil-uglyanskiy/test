import { t } from 'utils/localization';

const fields = [
  'policy_number'
];

const placeholders = {
  policy_number: t('Patients.Filters.Policy.Label')
};

export default {
  fields,
  placeholders
};
