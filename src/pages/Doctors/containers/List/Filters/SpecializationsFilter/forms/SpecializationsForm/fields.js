import { t } from 'utils/localization';

const fields = [
  'specialization'
];

const placeholders = {
  specialization: t('Doctors.Filters.Specializations.Label')
};

export default {
  fields,
  placeholders
};
